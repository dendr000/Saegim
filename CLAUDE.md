# 새김 (Saegim)

중학교 역사 수업용 퀴즈 게임 데스크톱 앱.

## 이름 표기 규칙

- 사용자에게 보이는 앱 이름은 항상 **새김** (한글). 창 제목, 화면 로고, 정보 표시 모두 한글.
- 코드·설정·파일명 등 식별자에는 **Saegim** 을 쓴다.
  - `package.json` name: `saegim` — **예외.** npm 패키지명은 대문자를 허용하지 않는
    강제 규칙이라 이것만 소문자로 남긴다.
  - electron-builder `productName`: `새김`, `appId`: `com.saegim.quiz` — appId는
    역방향 도메인 표기 관례상 소문자를 유지한다(브랜드 표기와 무관한 기술 식별자).
  - 빌드 산출물: `Saegim.exe`
  - 저장소·폴더명: `Saegim`
- 코드 주석과 문서는 한글 '새김'으로 쓴다.
- 라이선스: MIT (루트 `LICENSE` 파일 참고). 저장소는 공개(public) 저장소다.

## 무엇을 만드는가

중학교 역사 교사가 수업 중 교실에서 사용하는 퀴즈 게임 앱.
윈도우 포터블 exe 하나로 배포하며, USB에 넣어 어느 노트북에서든 같은 데이터로 실행된다.
사용자는 교사 본인이며, 개발자가 아닌 동료 교사도 쓸 수 있어야 한다.

## 기술 스택 (확정)

- Electron + React 19 + Vite + TypeScript
- 패키징: electron-builder, `portable` 타겟 (단일 exe, 설치 불필요)
- 데이터: Node.js `fs` 기반 JSON 플랫 파일 DB
- 프론트↔백 통신: Electron IPC (contextBridge preload)

**SQLite / better-sqlite3 를 도입하지 말 것.** 네이티브 모듈 리빌드가 빌드 단계의 리스크다.
데이터 접근은 전부 `src/db/repositories/` 안에 격리해서, 훗날 교체 가능하게만 유지한다.

## 절대 규칙

1. **한국어로 응답한다.**
2. **기능별로 파일을 분리한다.** 한 파일이 여러 기능을 담지 않는다. 게임 모드는 1모드 = 1폴더.
3. 게임 로직은 응답의 출처를 몰라야 한다. 모든 게임은 정규화된 응답 객체만 받는다.
4. `data/` 폴더의 실제 데이터 파일은 git에 커밋하지 않는다. 샘플만 `data.sample/`에 둔다.
5. 큰 기능을 시작하기 전에 구현 계획을 먼저 제시하고 확인을 받는다.

## 폴더 구조

```
data/                          # 런타임 JSON DB (exe 옆에 위치, git 제외)
  questions.json
  classes.json
  sessions.json
  maps/                        # 단원별 역사 지도 SVG 프리셋
src/
  main/                        # Electron 메인 프로세스
    index.ts
    paths.ts                   # userData 경로를 exe 옆으로 재지정 (포터블 핵심)
    ipc/                       # 도메인별 IPC 핸들러
  preload/
    index.ts                   # contextBridge API 노출
  db/
    repositories/              # 데이터 접근 캡슐화 (교체 가능 계층)
  renderer/
    pages/                     # 화면 단위 (Home, QuestionBank, ClassManager, GameSelect, Result)
    games/
      _shared/                 # 타이머, 점수판, 정답 연출, 입력 계층, 효과음
      timeAttack/
      territory/
      betting/
    components/
    hooks/
  shared/
    types/                     # 메인·렌더러 공용 타입
```

## 문항 데이터 스키마

문항 유형이 계속 늘어난다. 유형별 데이터는 `payload`에 격리한다.

```ts
type Question = {
  id: string;
  era: string;        // 시대 (예: 삼국시대, 조선후기)
  unit: string;       // 단원
  difficulty: 1 | 2 | 3;
  type: QuestionType;
  payload: unknown;   // 유형별 구조
};

type QuestionType =
  | 'multipleChoice'   // 객관식
  | 'shortAnswer'      // 단답형
  | 'initialLetter'    // 초성 퀴즈
  | 'sourceReading'    // 사료 지문 해석
  | 'imageIdentify'    // 인물·유물 이미지 판별
  | 'timelineOrder';   // 연표 정렬
```

객관식 전용으로 스키마를 굳히지 말 것. 나중에 문제은행 전체를 마이그레이션해야 한다.

## 게임 모드

**v1 구현 대상 (이 순서로)**

1. **타임어택 콤보** — 제한시간 + 연속 정답 배수. 개인전.
2. **땅따먹기** — SVG 역사 지도 위 지역 칸을 골라 문제 풀고 점령. 팀 대항.
   추상 격자가 아니라 `data/maps/`의 실제 지도 SVG를 사용한다. 지도 교체로 단원 재사용.
3. **베팅형** — 문제 공개 **전에** 보유 점수 일부를 배팅. 최종 문제 역전이 핵심.

**향후 추가 (구조만 열어둘 것)**
개인전 / 전체 참여형
  타임어택 콤보 — 제한시간 + 연속 정답 배수. 기본형이지만 콤보가 끊기는 긴장감이 핵심
  골든벨 서바이벌 — 틀리면 탈락, 단 부활 문제 1회. 탈락자는 심판이나 응원단 역할을 주면 이탈 방지
  모자이크 공개 — 사진/그림이 서서히 선명해지고, 빨리 맞힐수록 고득점. 인물·지도·생물 단원에 특히 잘 맞음
  힌트 차감형 — 힌트를 쓸 때마다 배점이 깎임. 자기 실력을 스스로 계산하게 만듦
  초성 퀴즈 — 용어 암기 단원에 효율적

팀 대항형
  점수 베팅(더블 오어 나싱) — 문제 전에 보유 점수 일부를 걸게 함. 마지막 문제 올인으로 순위가 뒤집히니 끝까지 안 놓음
  땅따먹기 — 5×5 격자 중 원하는 칸을 골라 문제 풀고 점령. 세 줄 만들면 보너스. 칸 선택이라는 전략이 생김
  문제 빙고 — 위와 비슷하지만 문제 난이도를 칸에 미리 배치해서 보이게 함
  보스 레이드(협동형) — 반 전체가 HP를 가진 보스를 상대. 정답이 곧 데미지. 경쟁 대신 협력이 필요한 반에 유용
  폭탄 돌리기 — 타이머가 랜덤하게 터짐. 순서가 돌아오는 조마조마함이 실력과 무관하게 작동
  찬스카드 — 정답 시 랜덤 카드 획득(점수 훔치기, 순서 뒤집기, 방어막). 실력차를 흐리는 가장 강력한 장치

개념 학습형
  카드 매칭 — 용어와 정의를 짝짓기. 정답률보다 반복 노출이 목적
  분류 드래그 — 항목을 카테고리 박스로 끌어놓기. 생물 분류, 문학 갈래, 화학 반응 종류 등

설계 원칙: 상위권 학생이 결과를 독점하면 나머지가 이탈한다. 운과 전략의 개입은 의도된 것이다.

## 참여 방식

v1은 **교사 기기 단독 진행**. 학생 개인 기기 참여는 아직 만들지 않는다.
단, `games/_shared/` 에 입력 계층을 추상화해 두어 나중에 구현체만 교체하면 되게 한다.

```ts
type Answer = { participantId: string; value: unknown; submittedAt: number };
```

게임 모드는 이 객체만 소비한다. 교사 키보드에서 왔는지 WebSocket에서 왔는지 알지 못한다.

## 구현 순서

**1단계를 먼저 통과하지 않고 다음으로 넘어가지 말 것.**

1. 빈 Electron + React 프로젝트 부팅 → **포터블 exe 빌드 성공 확인**
   - `paths.ts`의 경로 재지정이 실제 빌드된 exe에서 작동하는지 검증
   - 기능 다 만들고 빌드하려다 막히는 것이 최악의 시나리오
2. 문항 CRUD + CSV 일괄 가져오기
   - CSV 붙여넣기는 최우선 기능이다. 이게 불편하면 앱을 아무도 안 쓴다.
3. 학급/학생·팀 관리
4. `games/_shared` 공통 엔진 (타이머, 점수판, 정답 연출, 입력 계층)
5. 타임어택 → 땅따먹기 → 베팅
6. 세션 기록 저장 + 문항별 오답 통계

## 진행 기록

구현 순서의 각 단계가 끝날 때마다 `doc/` 폴더에 요약 md 파일을 하나씩 남긴다.
- 파일명: `NN-주제.md` (단계 번호와 맞춘다. 예: `01-portable-build.md`)
- 내용: 무엇을 만들었는지, 왜 그렇게 결정했는지, 검증 결과, 다음에 주의할 점.
- `doc/CLAUDE.md`는 1단계 종료 시점의 스냅샷 사본이다. 실제 지침은 항상 루트의
  `CLAUDE.md`를 따르고, `doc/CLAUDE.md`는 갱신하지 않는다.

## 유의사항

- 교실에서 TV 또는 프로젝터로 띄운다. 글자와 버튼을 크게, 뒷자리에서 읽히도록. 또는 조절이 가능하도록.
- 게임 중 예외 상황(오답 처리 실수, 점수 수정)에 교사가 수동 개입할 수 있어야 한다.
- 코드 서명은 하지 않는다. 첫 실행 시 SmartScreen 경고가 뜨는 것은 허용된 동작이다.
